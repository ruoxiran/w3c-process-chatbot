{
  "name": "w3c-process-chatbot",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/ui"
  ],
  "scripts": {
    "test:api": "pytest apps/api",
    "dev:web": "npm --prefix apps/web run dev",
    "lint:web": "npm --prefix apps/web run lint"
  }
}

